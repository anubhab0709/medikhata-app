# Production (Vercel): use same-origin proxy so Safari keeps the session
VITE_API_URL=/api

# Local development against a remote API (cookies may not stick in Safari)
# VITE_API_URL=https://medikhata-app.onrender.com/api
